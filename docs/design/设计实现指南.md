# ⚡ 设计实现指南

> Tailwind CSS 工具类与组件实现指南  
> 基于 [全局设计规范](./全局设计规范.md) 的具体技术实现

**版本:** 2.0.0 (Tailwind 适配版)  
**更新时间:** 2025-12-23  
**技术栈:** Next.js 14 + TypeScript 5 + Tailwind CSS 3

---

## 📑 快速导航

- [Tailwind 工具类速查](#-tailwind-工具类速查)
- [组件实现示例](#-组件实现示例)
- [TypeScript Token 使用](#-typescript-token-使用)
- [响应式设计](#-响应式设计)
- [迁移指南](#-迁移指南)

---

## 🚀 Tailwind 工具类速查

### 🎨 颜色系统

#### 主色调与状态色
```css
/* 品牌主色系统 */
.bg-primary              /* #a02724 - 品牌主色 */
.bg-tesla                 /* #0070c0 - 特斯拉风格选中色 */

/* 状态色彩系统 */
.bg-status-good           /* #00b050 - 优秀状态 */
.bg-status-warning        /* #ffc000 - 预警状态 */
.bg-status-danger         /* #c00000 - 危险状态 */
.bg-status-normal         /* #666666 - 正常状态 */

/* 文字色彩系统 */
.text-text-primary        /* #333333 - 主要文字 */
.text-text-secondary      /* #666666 - 次要文字 */
.text-text-muted          /* #999999 - 辅助文字 */
.text-text-inverse        /* #ffffff - 反色文字 */

/* 边框色彩系统 */
.border-border-light      /* #e0e0e0 - 浅边框 */
.border-border-medium     /* #cccccc - 中等边框 */
.border-border-dark       /* #999999 - 深边框 */
```

#### 图表专用色
```css
/* 图表专用色彩 */
.bg-chart-claim-rate     /* #87ceeb - 满期赔付率柱 */
.bg-chart-expense-rate  /* #d3d3d3 - 费用率柱 */
.text-chart-progress-line /* #808080 - 达成率折线 */

/* 数据深度色彩 */
.bg-data-depth-light     /* #e6f3ff - 浅度数据色 */
.bg-data-depth-medium    /* #4d94ff - 中度数据色 */
.bg-data-depth-dark      /* #0066cc - 深度数据色 */
```

### 📏 间距系统

```css
/* 基础间距 */
.p-xs, .m-xs            /* 4px  - 微间距 */
.p-sm, .m-sm            /* 8px  - 按钮内边距 */
.p-md, .m-md            /* 16px - 卡片间距 */
.p-lg, .m-lg            /* 24px - 区块间距 */
.p-xl, .m-xl            /* 32px - 大区块间距 */
.p-xxl, .m-xxl          /* 48px - 页面主体间距 */
```

### 🔄 圆角系统

```css
.rounded-xs             /* 4px  - 小圆角 */
.rounded-sm             /* 8px  - 按钮、输入框 */
.rounded-md             /* 12px - 卡片容器 */
.rounded-lg             /* 16px - 大型容器 */
.rounded-xl             /* 20px - KPI卡片 */
.rounded-full           /* 50%  - 圆形元素 */
```

### 🌟 阴影系统

```css
.shadow-sm              /* 轻微悬浮 - 表单聚焦 */
.shadow-md              /* 卡片层级 - KPI卡片、筛选器 */
.shadow-lg              /* 弹出层级 - 下拉面板 */
.shadow-xl              /* 模态层级 - 对话框 */
.shadow-focus           /* 焦点状态 - 键盘焦点光晕 */
```

### ⏱️ 动画与过渡

```css
/* 动画时长 */
.duration-fast          /* 150ms - 快速动画 */
.duration-normal        /* 250ms - 标准动画 */
.duration-slow          /* 350ms - 慢速动画 */

/* 缓动函数 */
.ease-out               /* cubic-bezier(0.25, 0.46, 0.45, 0.94) */
.ease-in-out            /* cubic-bezier(0.4, 0, 0.2, 1) */

/* 通用过渡 */
.transition-fast        /* all 150ms ease-out */
.transition-normal      /* all 250ms ease-out */
.transition-slow        /* all 350ms ease-in-out */
```

---

## 🧩 组件实现示例

### 📊 KPI 卡片组件

```tsx
interface KpiCardProps {
  title: string;              // 卡片标题
  value: string | number;     // 指标值
  variant?: 'default' | 'good' | 'warning' | 'danger';
  hint?: string;              // 提示文本
  onClick?: () => void;       // 点击事件
}

/**
 * KPI卡片组件 - 遵循全局设计规范
 * 实现文件: src/components/kpi/KpiCard.tsx
 */
export function KpiCard({ 
  title, 
  value, 
  variant = 'default', 
  hint,
  onClick 
}: KpiCardProps) {
  // 根据状态获取背景色
  const getVariantStyles = () => {
    switch (variant) {
      case 'good':
        return 'bg-status-good/10 border-status-good/20';
      case 'warning':
        return 'bg-status-warning/10 border-status-warning/20';
      case 'danger':
        return 'bg-status-danger/10 border-status-danger/20';
      default:
        return 'bg-white border-border-light';
    }
  };

  // 根据状态获取数值颜色
  const getValueColor = () => {
    switch (variant) {
      case 'good': return 'text-status-good';
      case 'warning': return 'text-status-warning';
      case 'danger': return 'text-status-danger';
      default: return 'text-text-primary';
    }
  };

  return (
    <div 
      className={cn(
        'rounded-xl p-4 border shadow-md transition-normal hover:shadow-lg',
        'cursor-pointer backdrop-blur-sm',
        getVariantStyles(),
        onClick && 'hover:-translate-y-1'
      )}
      onClick={onClick}
    >
      {/* 标题区域 */}
      <div className="text-xs text-text-secondary font-medium mb-2">
        {title}
      </div>
      
      {/* 数值区域 */}
      <div className={cn(
        'text-3xl font-bold leading-tight tracking-tight mb-2',
        getValueColor()
      )}>
        {typeof value === 'number' ? value.toLocaleString() : value}
      </div>
      
      {/* 提示区域 */}
      {hint && (
        <div className="text-xs text-text-muted">
          {hint}
        </div>
      )}
    </div>
  );
}
```

### 🎛️ 筛选器组件

```tsx
interface FilterSelectorProps<T> {
  label: string;
  value: T;
  onChange: (value: T) => void;
  options: Array<{ value: T; label: string }>;
  placeholder?: string;
}

/**
 * 筛选器选择组件
 * 实现文件: src/components/ui/FilterSelector.tsx
 */
export function FilterSelector<T>({
  label,
  value,
  onChange,
  options,
  placeholder = "请选择"
}: FilterSelectorProps<T>) {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="relative">
      {/* 标签 */}
      <div className="text-xs text-text-secondary mb-1 font-medium">
        {label}
      </div>
      
      {/* 选择器 */}
      <button
        className={cn(
          'flex items-center justify-between w-full px-3 py-2',
          'bg-white/90 border border-border-light rounded-sm',
          'text-text-primary text-sm transition-normal',
          'hover:border-tesla hover:shadow-sm',
          isOpen && 'border-tesla shadow-focus',
          value && 'bg-tesla/5 border-tesla text-tesla'
        )}
        onClick={() => setIsOpen(!isOpen)}
      >
        <span>
          {value 
            ? options.find(opt => opt.value === value)?.label || placeholder
            : placeholder
          }
        </span>
        <svg 
          className={cn('w-4 h-4 transition-transform', isOpen && 'rotate-180')}
          fill="none" 
          stroke="currentColor" 
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      {/* 下拉面板 */}
      {isOpen && (
        <div className="absolute top-full left-0 right-0 z-50 mt-1">
          <div className="bg-white rounded-md shadow-lg border border-border-light">
            {options.map((option) => (
              <button
                key={String(option.value)}
                className={cn(
                  'w-full px-3 py-2 text-left text-sm transition-normal',
                  'hover:bg-tesla/5 hover:text-tesla',
                  option.value === value && 'bg-tesla text-text-inverse',
                  'first:rounded-t-md last:rounded-b-md'
                )}
                onClick={() => {
                  onChange(option.value);
                  setIsOpen(false);
                }}
              >
                {option.label}
              </button>
            ))}
          </div>
        </div>
      )}
    </div>
  );
}
```

### 📈 图表容器组件

**图表标题命名规则（总览页）**
- 标题需包含视角前缀：全省 / 同城 / 异地 / 机构名称
- 示例：全省月度保费规划图、同城季度占比规划图、德阳月度占比规划图

```tsx
interface ChartContainerProps {
  title?: string;
  subtitle?: string;
  height?: 'sm' | 'md' | 'lg' | number;
  children: React.ReactNode;
  actions?: React.ReactNode;
}

/**
 * 图表容器组件
 * 实现文件: src/components/charts/ChartContainer.tsx
 */
export function ChartContainer({
  title,
  subtitle,
  height = 'lg',
  children,
  actions
}: ChartContainerProps) {
  // 高度映射
  const heightMap = {
    sm: 'h-96',      // 400px
    md: 'h-[500px]', // 500px
    lg: 'h-[600px]'  // 600px
  };

  const chartHeight = typeof height === 'number' 
    ? `h-[${height}px]` 
    : heightMap[height];

  return (
    <div className="rounded-xl border border-border-light p-4 bg-white shadow-md">
      {/* 头部区域 */}
      {(title || subtitle || actions) && (
        <div className="flex items-center justify-between mb-4">
          <div>
            {title && (
              <h3 className="text-base font-semibold text-text-primary">
                {title}
              </h3>
            )}
            {subtitle && (
              <p className="text-sm text-text-secondary mt-1">
                {subtitle}
              </p>
            )}
          </div>
          {actions && (
            <div className="flex items-center gap-2">
              {actions}
            </div>
          )}
        </div>
      )}

      {/* 图表区域 */}
      <div className={cn('relative', chartHeight)}>
        {children}
      </div>
    </div>
  );
}
```

---

## 🔧 TypeScript Token 使用

### 🎨 颜色 Token

```typescript
import { colors } from '@/styles/tokens';

// 使用颜色 token
function getStatusColor(status: 'good' | 'warning' | 'danger') {
  switch (status) {
    case 'good':
      return colors.status.good;     // #00b050
    case 'warning':
      return colors.status.warning;  // #ffc000
    case 'danger':
      return colors.status.danger;   // #c00000
    default:
      return colors.text.primary;    // #333333
  }
}

// 图表专用色
const chartConfig = {
  claimRate: colors.chart.claimRate,    // #87ceeb
  expenseRate: colors.chart.expenseRate, // #d3d3d3
  progressLine: colors.chart.progressLine, // #808080
};
```

### 📏 间距与尺寸 Token

```typescript
import { spacing, typography } from '@/styles/tokens';

// 使用间距 token
const cardStyles = {
  padding: spacing.md,      // 16px
  margin: spacing.sm,       // 8px
  borderRadius: spacing.lg, // 24px (使用间距作为圆角)
};

// 使用字体 token
const textStyles = {
  fontSize: typography.sizes.base,  // 14px
  fontWeight: typography.weights.medium, // 500
  lineHeight: typography.heights.normal, // 1.4
};
```

### 🎯 工具函数

```typescript
import { 
  getDataDepthColor, 
  getStatusColor, 
  cn 
} from '@/lib/utils';

// 根据数值获取深度颜色
function applyDataDepth(value: number, maxValue: number) {
  const color = getDataDepthColor(value, maxValue);
  return `bg-${color}`;
}

// 根据指标值获取状态颜色
function getKpiVariant(value: number, thresholds: { good: number; warning: number }) {
  const status = getStatusColor(value, thresholds);
  return status;
}

// 条件类名合并
function getButtonStyles(isActive: boolean, isDisabled: boolean) {
  return cn(
    'px-3 py-2 rounded-sm transition-normal',
    isActive && 'bg-tesla text-text-inverse',
    isDisabled && 'opacity-50 cursor-not-allowed',
    !isActive && !isDisabled && 'hover:border-tesla'
  );
}
```

---

## 📱 响应式设计

### 🖥️ 断点系统

```css
/* Tailwind 默认断点 */
sm: 640px    /* 小屏幕 */
md: 768px    /* 平板 */
lg: 1024px   /* 桌面 */
xl: 1280px   /* 大桌面 */
2xl: 1536px  /* 超大屏幕 */

/* 项目自定义断点（在 tailwind.config.js 中扩展） */
3xl: 1920px  /* 大屏显示 */
4xl: 2560px  /* 超大屏显示 */
```

### 📊 网格布局

```tsx
// KPI 卡片响应式网格
<div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-4 2xl:grid-cols-6">
  <KpiCard title="年度目标" value="12345" />
  <KpiCard title="YTD目标" value="9876" />
  {/* 更多卡片... */}
</div>

// 图表区域响应式布局
<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
  <ChartContainer title="月度目标分解" height="lg">
    <MonthlyBarChart />
  </ChartContainer>
  <ChartContainer title="季度目标" height="lg">
    <QuarterlyBarChart />
  </ChartContainer>
</div>

// 筛选器响应式布局
<div className="flex flex-col sm:flex-row gap-4 p-4 bg-white/90 rounded-xl">
  <FilterSelector label="产品" value={product} onChange={setProduct} />
  <FilterSelector label="机构" value={org} onChange={setOrg} />
  <FilterSelector label="时间" value={time} onChange={setTime} />
</div>
```

### 📏 大屏适配

```tsx
// PPT 容器适配 (2400px)
<div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100">
  <div className="max-w-[2400px] mx-auto p-6">
    {/* 内容区适配 (2100px) */}
    <div className="max-w-[2100px] mx-auto">
      {/* 主要内容 */}
    </div>
  </div>
</div>

// 图表高度适配
<ChartContainer 
  title="经营概览" 
  height="lg" // 600px 标准高度
>
  <BusinessOverviewChart />
</ChartContainer>
```

---

## 🔄 迁移指南

### 📊 从旧样式迁移

#### 颜色迁移对照表

| 旧 Tailwind 类 | 新设计系统类 | 说明 |
|---------------|-------------|------|
| `text-slate-600` | `text-text-secondary` | 次要文字 |
| `text-slate-900` | `text-text-primary` | 主要文字 |
| `text-blue-700` | `text-tesla` | 特斯拉蓝 |
| `bg-slate-50` | `bg-secondary` | 次要背景 |
| `border-slate-200` | `border-border-light` | 浅边框 |
| `bg-red-500` | `bg-status-danger` | 危险状态 |
| `bg-green-500` | `bg-status-good` | 优秀状态 |
| `bg-yellow-500` | `bg-status-warning` | 预警状态 |

#### 组件迁移示例

**迁移前的卡片:**
```tsx
<div className="rounded-xl border border-slate-200 p-4 bg-white shadow-sm">
  <div className="text-xs text-slate-600 font-medium">{title}</div>
  <div className="mt-2 text-lg font-semibold text-slate-900">{value}</div>
</div>
```

**迁移后的卡片:**
```tsx
<KpiCard 
  title={title} 
  value={value} 
  variant={variant}
  onClick={handleClick}
/>
```

#### 筛选器迁移示例

**迁移前的选择器:**
```tsx
<select 
  value={product} 
  onChange={(e) => setProduct(e.target.value)}
  className="px-3 py-2 border border-slate-200 rounded-md"
>
  <option value="total">汇总</option>
  <option value="auto">车险</option>
</select>
```

**迁移后的选择器:**
```tsx
<FilterSelector
  label="产品"
  value={product}
  onChange={setProduct}
  options={[
    { value: 'total', label: '汇总' },
    { value: 'auto', label: '车险' },
  ]}
/>
```

### 🧪 测试与验证

#### 视觉回归测试
```bash
# 运行视觉回归测试
npm run test:visual

# 检查组件库快照
npm run test:components
```

#### 设计规范验证
```typescript
// 自动验证设计规范合规性
import { validateDesignTokens } from '@/styles/validation';

// 验证所有组件使用的设计 token
const validationResults = validateDesignTokens();
console.log('设计规范验证结果:', validationResults);
```

---

## 🛠️ 实用工具

### 🎨 cn 工具函数

```typescript
import { cn } from '@/lib/utils';

// 条件类名应用
const buttonClass = cn(
  'base-button-styles',
  isActive && 'active-styles',
  isDisabled && 'disabled-styles'
);

// 类冲突解决（后者优先）
const result = cn('px-2', 'px-4'); // 结果: 'px-4'

// 动态类名生成
function getCardStyles(variant: 'default' | 'highlighted') {
  return cn(
    'card-base-styles',
    variant === 'highlighted' && 'ring-2 ring-tesla'
  );
}
```

### 📏 样式调试工具

```typescript
// 开发环境样式调试
if (process.env.NODE_ENV === 'development') {
  // 显示设计 token 信息
  window.debugTokens = () => {
    console.log('Color Tokens:', colors);
    console.log('Spacing Tokens:', spacing);
    console.log('Typography Tokens:', typography);
  };
  
  // 高亮未使用的设计系统样式
  window.highlightLegacyStyles = () => {
    // 实现未使用样式的检测和高亮
  };
}
```

---

**文档版本:** 2.0.0 (Tailwind 适配版)  
**更新时间:** 2025-12-23  
**维护团队:** 前端开发团队  
**相关文档**: [全局设计规范](./全局设计规范.md) | [组件设计规范](./组件设计规范.md)
